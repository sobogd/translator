// Shared vertical spacing of the desktop chrome, in px.
//
// One value everywhere — between the header and the first (translator)
// window, between the two windows, and between the translator's own blocks
// (history / languages / conversation / composer) — so the whole layout
// breathes with one rhythm. It matches the header's own inset from the top
// of the screen (the taskbar sits on p-2 = 8px).
export const LAYOUT_GAP = 8;
